/**
 * Alias for {@link SrToDeliverOrderForm}. Kept so imports of this path (e.g. older routes or cache) keep working.
 */
export { SrToDeliverOrderForm as SrDeliverForm } from "./SrToDeliverOrderForm";
